(ns quantum.test.validate.regex
  (:require [quantum.validate.regex :as ns]))

#?(:clj
(defn test:validate
  ([regexs])
  ([regexs-0 case-sensitive?])))

#?(:clj
(defn test:valid?
  [s patterns]))

#?(:clj
(defn test:match
  [s patterns]))