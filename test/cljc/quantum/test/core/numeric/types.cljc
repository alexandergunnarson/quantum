(ns quantum.test.core.numeric.types
  (:require [quantum.core.numeric.types :as ns]))

(defn test:number [x])

(defn test:com.gfredericks.goog.math.Integer [x])

(defn test:->bigint
  ([x]))

(defn test:ratio [x])

(defn test:normalize
  [n d])

(defn test:->ratio
  ([x])
  ([x y]))

(defn test:numerator [x])
(defn test:denominator [x])