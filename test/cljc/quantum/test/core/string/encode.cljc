(ns quantum.test.core.string.encode
  (:require [quantum.core.string.encode :as ns]))

; ===== ENCODINGS & CHARSETS =====

(defn test:ascii?
  [s])

(defn test:unicode->ascii
  [input & [silent-fail?]])