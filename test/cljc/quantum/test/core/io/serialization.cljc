(ns quantum.test.core.io.serialization
  (:require [quantum.core.io.serialization :as ns]))

(defn test:assoc-integral-values!
  [data-input integral-keys record-in])

(defn test:assoc-extra-kvs!
  [data-input record-in])

(defn test:extend-serialization-for-record!
  [record-type & [type-id-0]])

(defn test:extend-serialization-for-records!
  [& records])