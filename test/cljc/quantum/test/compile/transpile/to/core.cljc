(ns quantum.test.compile.transpile.to.core
  (:require [quantum.compile.transpile.to.core :as ns]))

(defn test:demunge-class [x])

(defn test:replace-specials [^String s-0 & [upper?]])

(defn ^String test:fn-pos
  ([fn-str])
  ([fn-str & args])

(defn test:enc-class [obj])

(defn test:elem-class [obj])

(defn test:obj-class [obj])

(defn test:do-form [form])

(defn test:apply-do-form [x])

(defn test:str-repeat [n s])

(defn test:countif [pred coll])

(defn test:line-height [s])

(defn test:vertical-spacing-from [prev-block])

(defn test:add-return-statements [expr])

(defn test:gen-return-statement
  [vertical-spacing body-in-do-form])

(defn test:infer-type [])

(defn test:scope-if-needed [expr])
 
(defn ^String test:eval-special-form [[special-sym :as form]])

(defn ^String test:eval-form [obj])

(defn ^String test:comma-splice
  ([arg])
  ([arg & args])


