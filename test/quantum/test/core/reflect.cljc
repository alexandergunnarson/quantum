(ns quantum.test.core.reflect
  (:require [quantum.core.reflect :as ns]))

(defn test:obj->map
  [obj])

(defn test:var-args? [f])

(defn test:var-arg-count
  [f])

(defn test:arg-count [f])

(defn test:invoke [obj method & args])

(defn test:enum-values
  [type])

(defn test:max-inputs
  [func num])

(defn test:qualified-name
  ([x]))
