(ns quantum.test.core.macros.deftype
  (:require [quantum.core.macros.deftype :as ns]))

; ===== |PROTOCOL|S & |REIFY|S =====

(defn test:pfn
  [sym lang])

(defn test:p-arity
  [sym arities])

(defn test:deftype-compatible
  [lang sym-0 arglist-0 skel-0])
