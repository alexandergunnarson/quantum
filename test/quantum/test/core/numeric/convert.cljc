(ns quantum.test.core.numeric.convert
  (:require [quantum.core.numeric.convert :as ns]))

(defn test:->big-integer [x])

(defn test:->bigint ([x]) ([x radix]))

(defn test:->bigdec [x])

(defn test:->ratio [x])

(defn test:exactly [x])