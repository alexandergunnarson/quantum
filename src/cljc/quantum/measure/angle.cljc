(ns quantum.measure.angle
  (:require [quantum.measure.core]))
  
; The angle subtended at the center of a circle by
; an arc equal in length to the radius of the
; circle.
#_(defunits-of angle [:radians #{:rad}])
