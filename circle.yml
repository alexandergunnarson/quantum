dependencies:
  pre:
    - sudo apt-get update
    - sudo apt install gcc-4.9 g++-4.9 ocl-icd-opencl-dev -y
test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) echo "lein test:clj" && lein test:clj ;; 1) echo "lein test:cljs" && lein test:cljs ;; esac:
        parallel: true
